let fib = fn(n) {
    return if n < 2 {
        n
    } else {
        fib(n-1) + fib(n-2)
    }
}

let in = takeLine("Enter a number")

let num = number!(in)
if error?(num) {
    writeLine(num)
    return 0
} else {
    writeLine(fib(num))
}
